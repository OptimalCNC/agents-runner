{
  "name": "agents-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web UI for running multiple Codex SDK run modes concurrently across git worktrees.",
  "scripts": {
    "start": "bun src/server.js",
    "dev": "bun --watch src/server.js",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "dependencies": {
    "@openai/codex": "^0.114.0",
    "@openai/codex-sdk": "^0.114.0"
  }
}
